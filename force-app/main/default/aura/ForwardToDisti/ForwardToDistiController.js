({
	doInit : function(component, event, helper) {
		helper.doInit(component, event, helper);
	},
    
	handleForwardToDisti : function(component, event, helper) {
		helper.handleForwardToDisti(component, event, helper);
	},
    
    handleCancel : function(component, event, helper) {
		helper.handleCancel(component, event, helper);
	},
    
    handleToNextStep : function(component, event, helper) {
		helper.handleToNextStep(component, event, helper);
	},
    
    handleBack: function(component, event, helper) {
		helper.handleBack(component, event, helper);
	},
    
    handleSearchContactByEmail: function(component, event, helper) {
        helper.handleSearchContactByEmail(component, event, helper);
    },
    
    handleAddContact: function(component, event, helper) {
        helper.handleAddContact(component, event, helper);
    },
    
    handleCreateNewContact: function(component, event, helper) {
        helper.handleCreateNewContact(component, event, helper);
    },
    
    handleSaveNewContact: function(component, event, helper) {
        helper.handleSaveNewContact(component, event, helper);
    },
    
    handleSaveNewContactSuccess: function(component, event, helper) {
        helper.handleSaveNewContactSuccess(component, event, helper);
    },
})